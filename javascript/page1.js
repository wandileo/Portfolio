var button = document.getElementById("questionButton").addEventListener("click", Hifunction);

function Hifunction() {
    var food = prompt("What is your favourite food?")
    alert("Your favourite food is " + food);
}